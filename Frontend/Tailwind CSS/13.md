# Chapter 13: Transitions & Animations

## 13.1 Understanding Transitions vs Animations

```
┌─────────────────────────────────────────────────────────────────┐
│              TRANSITIONS vs ANIMATIONS                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  TRANSITIONS:                                                   │
│  • Triggered by state change (hover, focus, class change)       │
│  • Smooth change from A → B                                     │
│  • Single direction (or both with return)                       │
│  • Simple property changes                                      │
│                                                                  │
│     Idle ────────────→ Hover                                    │
│     bg-blue  ─smooth─→ bg-red                                   │
│                                                                  │
│  ANIMATIONS:                                                    │
│  • Run automatically or on trigger                              │
│  • Can have multiple keyframes (A → B → C → D)                 │
│  • Can loop infinitely                                          │
│  • Complex multi-step sequences                                 │
│                                                                  │
│     ●──────●──────●──────●──────● (keyframes)                  │
│     0%    25%    50%    75%   100%                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 13.2 Transition Property

Control WHICH properties transition.

```html
<!-- No transition -->
<div class="transition-none">Instant changes</div>

<!-- All properties (heavy, use sparingly) -->
<div class="transition-all">Everything transitions</div>

<!-- Default (optimized set) -->
<div class="transition">
  Transitions: color, background-color, border-color, text-decoration-color,
  fill, stroke, opacity, box-shadow, transform, filter, backdrop-filter
</div>

<!-- Specific properties -->
<div class="transition-colors">Only colors transition</div>
<div class="transition-opacity">Only opacity transitions</div>
<div class="transition-shadow">Only shadows transition</div>
<div class="transition-transform">Only transforms transition</div>
```

### What Each Transition Covers

```
┌─────────────────────────────────────────────────────────────────┐
│                    TRANSITION PROPERTIES                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  transition-colors:                                             │
│    • color                                                      │
│    • background-color                                           │
│    • border-color                                               │
│    • text-decoration-color                                      │
│    • fill                                                       │
│    • stroke                                                     │
│                                                                  │
│  transition-opacity:                                            │
│    • opacity                                                    │
│                                                                  │
│  transition-shadow:                                             │
│    • box-shadow                                                 │
│                                                                  │
│  transition-transform:                                          │
│    • transform                                                  │
│                                                                  │
│  transition (default):                                          │
│    • All of the above + filter + backdrop-filter               │
│                                                                  │
│  transition-all:                                                │
│    • EVERY property (⚠️ can be slow)                           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 13.3 Transition Duration

Control HOW LONG transitions take.

```html
<div class="transition duration-0">Instant (0ms)</div>
<div class="transition duration-75">75ms (very fast)</div>
<div class="transition duration-100">100ms</div>
<div class="transition duration-150">150ms (snappy)</div>
<div class="transition duration-200">200ms (quick)</div>
<div class="transition duration-300">300ms (balanced)</div>
<div class="transition duration-500">500ms (noticeable)</div>
<div class="transition duration-700">700ms (slow)</div>
<div class="transition duration-1000">1000ms (very slow)</div>

<!-- Arbitrary values -->
<div class="transition duration-[400ms]">400ms</div>
<div class="transition duration-[2s]">2 seconds</div>
```

### Duration Guidelines

```
┌─────────────────────────────────────────────────────────────────┐
│                    DURATION GUIDELINES                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  75-100ms   │ Micro-interactions (button press, toggle)        │
│             │ Should feel instant but smooth                    │
│                                                                  │
│  150-200ms  │ Small elements (icons, small buttons)            │
│             │ Quick but perceptible                             │
│                                                                  │
│  200-300ms  │ Medium elements (cards, dropdowns)               │
│             │ The sweet spot for most UI                        │
│                                                                  │
│  300-500ms  │ Large elements (modals, page transitions)        │
│             │ Feels deliberate                                  │
│                                                                  │
│  500ms+     │ Complex animations, attention-grabbing           │
│             │ Use sparingly                                     │
│                                                                  │
│  ⚠️ Rule of thumb: Faster is usually better for UI             │
│     Users perceive delays over 100ms as "slow"                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 13.4 Transition Timing Function

Control the ACCELERATION CURVE.

```html
<div class="transition ease-linear">Constant speed</div>
<div class="transition ease-in">Slow start, fast end</div>
<div class="transition ease-out">Fast start, slow end</div>
<div class="transition ease-in-out">Slow start and end</div>

<!-- Arbitrary cubic-bezier -->
<div class="transition ease-[cubic-bezier(0.68,-0.55,0.265,1.55)]">
  Custom bounce
</div>
```

### Timing Function Visualized

```
┌─────────────────────────────────────────────────────────────────┐
│                    EASING CURVES                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ease-linear:          ease-in:              ease-out:          │
│  ┌──────────────┐     ┌──────────────┐      ┌──────────────┐   │
│  │            ╱ │     │          ╱╱  │      │    ╱╱        │   │
│  │          ╱   │     │        ╱╱    │      │  ╱╱          │   │
│  │        ╱     │     │      ╱╱      │      │╱╱            │   │
│  │      ╱       │     │   ╱╱╱        │      │              │   │
│  │    ╱         │     │ ╱╱           │      │              │   │
│  │  ╱           │     │╱             │      │              │   │
│  └──────────────┘     └──────────────┘      └──────────────┘   │
│  Constant speed       Accelerates           Decelerates         │
│                                                                  │
│  ease-in-out:         Custom bounce:                            │
│  ┌──────────────┐     ┌──────────────┐                         │
│  │         ╱╱   │     │    ╱───╲     │                         │
│  │       ╱╱     │     │   ╱     ╲    │                         │
│  │     ╱╱       │     │  ╱       ╲   │                         │
│  │   ╱╱         │     │ ╱         ╲──│                         │
│  │ ╱╱           │     │╱             │                         │
│  └──────────────┘     └──────────────┘                         │
│  Slow-fast-slow       Overshoots                               │
│                                                                  │
│  When to use each:                                              │
│  • ease-out: Most UI (things coming in)                        │
│  • ease-in: Things exiting/dismissing                          │
│  • ease-in-out: Things that loop or morph                      │
│  • linear: Loading spinners, constant motion                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 13.5 Transition Delay

Control WHEN the transition starts.

```html
<div class="transition delay-0">No delay (default)</div>
<div class="transition delay-75">75ms delay</div>
<div class="transition delay-100">100ms delay</div>
<div class="transition delay-150">150ms delay</div>
<div class="transition delay-200">200ms delay</div>
<div class="transition delay-300">300ms delay</div>
<div class="transition delay-500">500ms delay</div>
<div class="transition delay-700">700ms delay</div>
<div class="transition delay-1000">1 second delay</div>

<!-- Arbitrary delays -->
<div class="transition delay-[400ms]">400ms delay</div>
```

### Staggered Animation Example

```html
<!-- Staggered list items -->
<ul class="space-y-2">
  <li class="transition delay-[0ms] hover:translate-x-2">Item 1</li>
  <li class="transition delay-[50ms] hover:translate-x-2">Item 2</li>
  <li class="transition delay-[100ms] hover:translate-x-2">Item 3</li>
  <li class="transition delay-[150ms] hover:translate-x-2">Item 4</li>
</ul>

<!-- Staggered cards on hover -->
<div class="group flex gap-4">
  <div class="transition delay-0 group-hover:scale-105">Card 1</div>
  <div class="transition delay-75 group-hover:scale-105">Card 2</div>
  <div class="transition delay-150 group-hover:scale-105">Card 3</div>
</div>
```

---

## 13.6 Built-in Animations

Tailwind includes four pre-built animations.

### animate-spin

Continuous 360° rotation (loading spinners).

```html
<svg class="animate-spin h-5 w-5 text-blue-500" viewBox="0 0 24 24">
  <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" 
          stroke-width="4" fill="none"></circle>
  <path class="opacity-75" fill="currentColor" 
        d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
</svg>

<!-- Simple spinner -->
<div class="animate-spin w-8 h-8 border-4 border-blue-500 
            border-t-transparent rounded-full"></div>
```

### animate-ping

Radar-style pulse effect (notifications).

```html
<!-- Notification badge with ping -->
<span class="relative flex h-3 w-3">
  <span class="animate-ping absolute inline-flex h-full w-full 
               rounded-full bg-red-400 opacity-75"></span>
  <span class="relative inline-flex rounded-full h-3 w-3 bg-red-500"></span>
</span>

<!-- Recording indicator -->
<div class="flex items-center gap-2">
  <span class="relative flex h-3 w-3">
    <span class="animate-ping absolute h-full w-full rounded-full 
                 bg-red-500 opacity-75"></span>
    <span class="relative h-3 w-3 rounded-full bg-red-600"></span>
  </span>
  <span>Recording</span>
</div>
```

### animate-pulse

Gentle fade in/out (loading skeletons).

```html
<!-- Skeleton loader -->
<div class="animate-pulse space-y-4">
  <div class="h-4 bg-gray-300 rounded w-3/4"></div>
  <div class="h-4 bg-gray-300 rounded w-1/2"></div>
  <div class="h-4 bg-gray-300 rounded w-5/6"></div>
</div>

<!-- Card skeleton -->
<div class="animate-pulse bg-white rounded-lg p-4 shadow">
  <div class="flex space-x-4">
    <div class="rounded-full bg-gray-300 h-12 w-12"></div>
    <div class="flex-1 space-y-4 py-1">
      <div class="h-4 bg-gray-300 rounded w-3/4"></div>
      <div class="h-4 bg-gray-300 rounded w-1/2"></div>
    </div>
  </div>
</div>
```

### animate-bounce

Bouncing motion (scroll indicators, attention).

```html
<!-- Scroll down indicator -->
<div class="animate-bounce">
  <svg class="w-6 h-6 text-gray-400" fill="none" stroke="currentColor" 
       viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
          d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
  </svg>
</div>

<!-- Attention badge -->
<span class="animate-bounce inline-flex items-center px-2 py-1 
             rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
  New!
</span>
```

### animate-none

Remove animation.

```html
<!-- Disable animation conditionally -->
<div class="animate-spin md:animate-none">
  Spins on mobile, stops on desktop
</div>

<!-- With motion preference -->
<div class="animate-spin motion-reduce:animate-none">
  Respects reduced motion preference
</div>
```

---

## 13.7 Custom Animations

Define your own keyframe animations in config.

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      animation: {
        // Simple animations
        'fade-in': 'fadeIn 0.5s ease-out',
        'fade-out': 'fadeOut 0.5s ease-out',
        'slide-up': 'slideUp 0.3s ease-out',
        'slide-down': 'slideDown 0.3s ease-out',
        'slide-in-right': 'slideInRight 0.3s ease-out',
        'slide-in-left': 'slideInLeft 0.3s ease-out',
        'scale-in': 'scaleIn 0.2s ease-out',
        'scale-out': 'scaleOut 0.2s ease-out',
        
        // Looping animations
        'spin-slow': 'spin 3s linear infinite',
        'wiggle': 'wiggle 1s ease-in-out infinite',
        'float': 'float 3s ease-in-out infinite',
        
        // Complex animations
        'bounce-in': 'bounceIn 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55)',
        'shake': 'shake 0.5s ease-in-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        fadeOut: {
          '0%': { opacity: '1' },
          '100%': { opacity: '0' },
        },
        slideUp: {
          '0%': { transform: 'translateY(20px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        slideDown: {
          '0%': { transform: 'translateY(-20px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        slideInRight: {
          '0%': { transform: 'translateX(100%)', opacity: '0' },
          '100%': { transform: 'translateX(0)', opacity: '1' },
        },
        slideInLeft: {
          '0%': { transform: 'translateX(-100%)', opacity: '0' },
          '100%': { transform: 'translateX(0)', opacity: '1' },
        },
        scaleIn: {
          '0%': { transform: 'scale(0.9)', opacity: '0' },
          '100%': { transform: 'scale(1)', opacity: '1' },
        },
        scaleOut: {
          '0%': { transform: 'scale(1)', opacity: '1' },
          '100%': { transform: 'scale(0.9)', opacity: '0' },
        },
        wiggle: {
          '0%, 100%': { transform: 'rotate(-3deg)' },
          '50%': { transform: 'rotate(3deg)' },
        },
        float: {
          '0%, 100%': { transform: 'translateY(0)' },
          '50%': { transform: 'translateY(-10px)' },
        },
        bounceIn: {
          '0%': { transform: 'scale(0)', opacity: '0' },
          '50%': { transform: 'scale(1.1)' },
          '100%': { transform: 'scale(1)', opacity: '1' },
        },
        shake: {
          '0%, 100%': { transform: 'translateX(0)' },
          '10%, 30%, 50%, 70%, 90%': { transform: 'translateX(-5px)' },
          '20%, 40%, 60%, 80%': { transform: 'translateX(5px)' },
        },
      },
    },
  },
}
```

### Using Custom Animations

```html
<!-- Fade in on mount -->
<div class="animate-fade-in">Fades in</div>

<!-- Slide up modal -->
<div class="animate-slide-up">Modal content</div>

<!-- Wiggling attention element -->
<span class="animate-wiggle">🔔</span>

<!-- Floating element -->
<div class="animate-float">☁️ Floating cloud</div>

<!-- Error shake -->
<input class="animate-shake border-red-500">
```

### Arbitrary Animation Values

```html
<!-- Inline animation definition -->
<div class="animate-[fadeIn_0.5s_ease-out]">
  Arbitrary animation
</div>

<div class="animate-[wiggle_1s_ease-in-out_infinite]">
  Arbitrary looping
</div>

<!-- With delay -->
<div class="animate-[slideUp_0.3s_ease-out_0.2s_forwards]">
  With delay and fill mode
</div>
```

---

## 13.8 Animation Fill Modes & Play States

Control animation behavior with arbitrary values:

```html
<!-- Fill modes -->
<div class="animate-[fadeIn_0.5s_forwards]">
  Stays at end state (forwards)
</div>

<div class="animate-[fadeIn_0.5s_backwards]">
  Starts at first keyframe during delay (backwards)
</div>

<div class="animate-[fadeIn_0.5s_both]">
  Both forwards and backwards
</div>

<!-- Play state -->
<div class="animate-spin [animation-play-state:paused] hover:[animation-play-state:running]">
  Paused until hover
</div>

<!-- Iteration count -->
<div class="animate-[bounce_1s_3]">
  Bounces 3 times
</div>

<!-- Direction -->
<div class="animate-[slide_1s_infinite_alternate]">
  Alternates direction
</div>
```

---

## 13.9 Practical Examples

### Button with All States

```html
<button class="px-6 py-3 bg-blue-500 text-white font-semibold rounded-lg
               transition-all duration-200 ease-out
               hover:bg-blue-600 hover:scale-105 hover:-translate-y-0.5 hover:shadow-lg
               active:scale-95 active:translate-y-0
               focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2
               disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100">
  Complete Button
</button>
```

### Animated Modal

```html
<!-- Backdrop -->
<div class="fixed inset-0 bg-black/50 
            animate-[fadeIn_0.2s_ease-out]"></div>

<!-- Modal -->
<div class="fixed inset-0 flex items-center justify-center p-4">
  <div class="bg-white rounded-xl shadow-xl max-w-md w-full
              animate-[scaleIn_0.2s_ease-out]">
    <div class="p-6">
      <h2 class="text-xl font-bold">Modal Title</h2>
      <p class="mt-2 text-gray-600">Modal content here...</p>
    </div>
  </div>
</div>
```

### Loading States

```html
<!-- Button loading state -->
<button class="px-6 py-3 bg-blue-500 text-white rounded-lg 
               flex items-center gap-2 disabled:opacity-75" disabled>
  <svg class="animate-spin h-5 w-5" viewBox="0 0 24 24">
    <circle class="opacity-25" cx="12" cy="12" r="10" 
            stroke="currentColor" stroke-width="4" fill="none"></circle>
    <path class="opacity-75" fill="currentColor" 
          d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
  </svg>
  Loading...
</button>

<!-- Skeleton screen -->
<div class="space-y-4">
  <div class="animate-pulse flex space-x-4">
    <div class="rounded-full bg-gray-200 h-12 w-12"></div>
    <div class="flex-1 space-y-3 py-1">
      <div class="h-4 bg-gray-200 rounded w-3/4"></div>
      <div class="h-4 bg-gray-200 rounded w-1/2"></div>
    </div>
  </div>
</div>
```

### Notification Toast

```html
<div class="fixed bottom-4 right-4 
            animate-[slideInRight_0.3s_ease-out]">
  <div class="bg-white rounded-lg shadow-lg p-4 flex items-center gap-3">
    <span class="text-green-500">✓</span>
    <span>Successfully saved!</span>
  </div>
</div>
```

### Staggered List Animation

```html
<ul class="space-y-2">
  <li class="animate-[slideUp_0.3s_ease-out_forwards] opacity-0"
      style="animation-delay: 0ms">Item 1</li>
  <li class="animate-[slideUp_0.3s_ease-out_forwards] opacity-0"
      style="animation-delay: 100ms">Item 2</li>
  <li class="animate-[slideUp_0.3s_ease-out_forwards] opacity-0"
      style="animation-delay: 200ms">Item 3</li>
  <li class="animate-[slideUp_0.3s_ease-out_forwards] opacity-0"
      style="animation-delay: 300ms">Item 4</li>
</ul>
```

---

## 13.10 Performance Best Practices

```
┌─────────────────────────────────────────────────────────────────┐
│              ANIMATION PERFORMANCE TIPS                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ✅ GPU-Accelerated Properties (use these):                    │
│     • transform (translate, scale, rotate)                      │
│     • opacity                                                   │
│                                                                  │
│  ⚠️ Trigger Layout/Paint (avoid animating):                    │
│     • width, height                                             │
│     • margin, padding                                           │
│     • top, left, right, bottom                                  │
│     • font-size                                                 │
│                                                                  │
│  ❌ Very Expensive:                                             │
│     • box-shadow (animate opacity instead)                      │
│     • border-radius                                             │
│     • filter/backdrop-filter                                    │
│                                                                  │
│  Best Practices:                                                │
│  1. Use transform instead of position changes                   │
│  2. Use opacity for fade effects                                │
│  3. Keep animations under 300ms for UI                          │
│  4. Test on low-end devices                                     │
│  5. Respect prefers-reduced-motion                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💡 Interview Quick Tips

1. **"transition vs animation?"**
   - Transition: State change, A→B
   - Animation: Keyframe-based, can loop, multi-step

2. **"Best duration for UI?"**
   - 150-300ms for most interactions
   - Shorter is usually better

3. **"Which easing for what?"**
   - `ease-out`: Things entering
   - `ease-in`: Things leaving
   - `linear`: Continuous motion

4. **"GPU accelerated properties?"**
   - `transform` and `opacity` only
   - Avoid animating width, height, margins

5. **"How to respect reduced motion?"**
   - `motion-reduce:animate-none`
   - `motion-reduce:transition-none`
